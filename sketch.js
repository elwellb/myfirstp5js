function setup() {
  createCanvas(400, 400);
  rectMode(CENTER);
  noStroke();
}

function draw() {
  background("lightBlue");
  
  fill("red");
  rect(200, 200, 100, 100);

  fill("green");
  ellipse(30, 30, 30, );
}
